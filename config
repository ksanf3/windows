# check a file hash with the following command...
  - Get-Filehash -Path "[path to file]" -Algorithm SHA256





# run Sophia-Script-for-Windows from github
###########################################

- download the script via the terminal with...
  - irm script.sophi.app -useb | iex
- edit the Sophia.ps1 file to configure functions you want to run
- open the terminal as an administrator and run the following commands to run the script
  - Set-Location -Path "[path to sophia folder]"
  - Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force
  - .\Sophia.ps1





# install software from winget
##############################

# open the terminal and run "cmd", then run the following command...
winget install Bitwarden.Bitwarden && winget install eloston.ungoogled-chromium && winget install Discord.Discord && winget install rcmaehl.MSEdgeRedirect && winget install EpicGames.EpicGamesLauncher && winget install flux.flux && winget install GnuPG.GnuPG && winget install REALiX.HWiNFO && winget install Jackett.Jackett && winget install Joplin.Joplin && winget install LibreWolf.LibreWolf && winget install MullvadVPN.MullvadVPN && winget install PolyMC.PolyMC && winget install qBittorrent.qBittorrent && winget install AntoineAflalo.SoundSwitch && winget install Valve.Steam && winget install TorProject.TorBrowser && winget install Ubisoft.Connect && winget install VideoLAN.VLC





# install software from the internet
####################################

- amd website -> Drivers & Support -> Auto-Detect
- github -> Ventoy





# config
########
       
  # amd software
    - set graphics profile to "gaming" but enable "Radeon Enhanced Sync"
  
  
  
  # MSEdgeRedirect
    - open and configure



  # flux
    - set to "Color Fidelity"
    - disable "Backwards Alarm Clock"
  
  
  
  # qBittorrent
    - change the following preferences
            - Behavior
                - configure power management
            - Connection
                - use a port generated by your vpn for the "port used for incoming connections"
            - Bittorrent
                - enable anonymous mode
            - Advanced
                - set network interface to your vpn, and recheck torrents on completion
    - you can install a theme by looking at the qBittorrent wiki on github
    - "View" tab -> Search Engine
    - configure search by...
        - jackett was already installed through winget, but you need to launch it to enable it, and set it to auto start on boot
        - to install the plugin and configure jackett, open qbittorrent and open the "Search" tab -> search plugins -> click on the link -> click on "How to configure Jackett plugin"
        - open the plugin and copy the url, then open qbittorrent and open the "Search" tab -> search plugins -> install a new one -> Web link -> paste the plugin url
        - configure jackett by reading the rest of the github page
        
        
        
  # vlc
    - you can install a theme by opening vlc -> tools -> preferences -> use custom theme -> click on the link

    
  
  # windows
    - if dual booting with another os, you need to configure windows to use UTC time, this is so the clock stays correct when switching between os's
      - you can do this by running the following command from an admin command prompt...
        - reg add "HKEY_LOCAL_MACHINE\System\CurrentControlSet\Control\TimeZoneInformation" /v RealTimeIsUniversal /d 1 /t REG_DWORD /f
    - in file explorer, pin the path "C:\ProgramData\Microsoft\Windows\Start Menu\Programs" to Quick access
            
 
 


# game paths
############
 
# fortnite
C:\Program Files\Epic Games\Fortnite\FortniteGame\Binaries\Win64\FortniteClient-Win64-Shipping.exe

# rocket league
C:\Program Files\Epic Games\rocketleague\Binaries\Win64\RocketLeague.exe

# trackmania
C:\Program Files\Epic Games\TrackmaniaNext\Trackmania.exe

